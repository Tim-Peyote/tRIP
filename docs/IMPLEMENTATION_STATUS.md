# TRip — implementation status

Последнее обновление: 2026-08-21

Движок: Godot 4.7.2 stable

Renderer: Forward+ / Metal 4.0 на Apple M1

## Milestone 0 — implemented

### Project foundation

- Рабочий `project.godot` с main scene, input actions, physics layers и shader globals.
- Git-репозиторий, `.gitignore` и `.gitattributes`.
- Верхнеуровневый SceneTree: frontend, session roots, UI, presentation и debug.
- Main Menu с живым 3D-лагерем, базовой навигацией и настройками.

### Persistent road laboratory menu

- Фон главного меню теперь является отдельным 3D-миром в `SubViewport`: ночная алтайская поляна, костёр, полевая варочная станция и сидящий исследователь.
- Лагерь читает реальное сохранение и визуально показывает мета-прогресс без отдельного экрана характеристик.
- Завершённые циклы и сюжетные открытия добавляют сушилку, фильтр спор, дистиллятор и корневой резонатор.
- После возвращения из вылазки меню обновляется сразу после сохранения.
- Композиция использует три плана, цветной туман, холодный лунный контровой свет и тёплый динамический свет костра.

### Autoload services

- `SceneRouter` — проверяемая смена PackedScene.
- `SaveService` — versioned JSON envelope и запись через temporary file.
- `SettingsService` — ConfigFile, defaults и применение аудиогромкости.
- `ContentDB` — рекурсивная регистрация content Resources и проверка уникальных ID.

### Data-driven contracts

- `ContentDefinition`
- `IngredientDefinition`
- `ItemDefinition`
- `EffectDefinition`
- `RecipeDefinition` / `RecipeStepDefinition`
- `HypothesisDefinition`
- `VisualProfile`
- `AudioProfile`

ID используют namespaces (`ingredient.*`, `item.*`, `recipe.*`, `effect.*`). Runtime-состояние вынесено в отдельные domain objects.

### Domain and components

- `ItemInstance`
- `CookingProcess` / `CookingProcessEvent`
- `RecipeResolver` / `RecipeResolution`
- `InteractableComponent`
- `NoiseEmitterComponent` / `GameplayNoiseEvent`

### Presentation and audio

- `PresentationSnapshot` отделяет gameplay state от экранного исполнения.
- `PresentationDirector` сглаживает стабильный набор shader global channels.
- `AudioDirector` переключает snapshots независимо от gameplay noise.
- Созданы 10 шин: Master, Music, UI, PlayerFoley, World, Ambience, Creatures, Interactions, Voice, Perception.
- Visual intensity сохраняется и уже влияет на shader главного меню.

### First content thread

- `ingredient.mooncap`
- `effect.spore_sight`
- `item.spore_sight_brew`
- `recipe.spore_sight_brew`

Эталонный процесс `grind → heat` проходит через настоящий RecipeResolver и выдаёт качество `PURE`.

## Verification

Smoke test:

```bash
/Applications/Godot.app/Contents/MacOS/Godot \
  --headless \
  --path /Users/shaman/Desktop/Projects/Godot/TRIP \
  res://core/tests/smoke_test.tscn
```

Ожидаемый результат: `TRip smoke test: PASS`.

Visual capture:

```bash
/Applications/Godot.app/Contents/MacOS/Godot \
  --path /Users/shaman/Desktop/Projects/Godot/TRIP \
  res://core/tests/visual_capture.tscn
```

Сцена сохраняет `/tmp/trip_visual_capture.png` и завершает процесс.

## Milestone 1 — playable foundation implemented

- `CharacterBody3D` controller с acceleration, gravity, sprint и crouch.
- Crouch меняет camera height и capsule; clearance ray не даёт встать под препятствием.
- Mouse look и InputMap gamepad defaults для обоих стиков и основных действий.
- Независимый camera rig и временный low-poly viewmodel рук.
- `InteractionOrchestrator`: raycast, focus, единый prompt, hold/cancel и inspect.
- `InventoryComponent` с mass/volume contract.
- Физическая сцена лунной шляпки получает имя и описание через `ContentDB`.
- Gameplay HUD: reticle, prompt, hold progress, inspect card, сумка и pause panel.
- Blockout убежища с физикой, светом, столом и первой точкой взаимодействия.
- Процедурный ambience играет через шину `Ambience`.
- Шаги создают отдельные `GameplayNoiseEvent`; звук не является источником AI-шума.
- Main Menu → Shelter → Pause → Main Menu работает без смены архитектурных контрактов.

Gameplay test проверяет физический raycast, data-driven prompt/inspection и перенос собранного предмета в сумку.

## Remaining Milestone 1 polish

- настроить ощущение acceleration, sprint и head bob на живом управлении;
- заменить blockout viewmodel на rigged руки и предмет в руке;
- добавить surface-based footstep cues и glyph switching;
- сделать полноценный режим осмотра с вращением предмета;
- провести десятиминутный playtest на motion comfort.

## Cooking/effect vertical thread — implemented

Рабочая цепочка:

```text
ingredient.mooncap
  → Mortar / grind
  → Cauldron / heat
  → RecipeResolver / PURE
  → item.spore_sight_brew
  → quick use
  → effect.spore_sight
  → screen perception + hidden world geometry
```

### Inventory

- Добавлены удаление предмета, расходники и сигнал использования.
- `ConsumableDefinition` хранит `effect_ids` и число доз.
- `B` показывает содержимое сумки, `1` применяет первый готовый состав.

### Physical cooking

- `CookingOrchestrator` владеет одним runtime `CookingProcess` и выбранным `RecipeDefinition`.
- `CookingToolComponent` конфигурирует физический инструмент данными: operation, ingredient, tags, amount, temperature, duration и required item.
- Ступка и котёл не имеют уникальной рецептурной логики.
- Неверный порядок отклоняется с объяснимым сообщением.
- Ступка действительно удаляет лунную шляпку из сумки.
- Resolver создаёт готовый предмет только для качества `WORKING` и выше.

### Effect execution

- `EffectOrchestrator` хранит активные эффекты и их длительность.
- Gameplay и presentation channels агрегируются отдельно.
- `PresentationSnapshot` управляет shader globals.
- Full-screen perception pass добавляет chroma separation, tracking, scanlines, noise и palette quantization.
- `spore_vision` открывает реальную world-space геометрию мицелия; shader не является gameplay truth.

### Verification

- `TRip smoke test: PASS`
- `TRip gameplay test: PASS`
- `TRip cooking/effect test: PASS`
- Обычное состояние и спорозрение проверены через GPU visual capture в Forward+/Metal.

## Tool/harvest/forest vertical thread — implemented

### Equipment

- `ToolDefinition` описывает capabilities и precision.
- `ToolbeltComponent` не зависит от конкретного ножа.
- `tool.field_knife` даёт `cut`, `separate_cap` и `separate_stem`.
- `Q` достаёт/убирает инструмент; состояние синхронизировано с viewmodel и HUD.

### Harvest decisions

- ПКМ циклически выбирает `cap / stem / whole / spores`.
- Prompt сообщает выбранную часть и отсутствие нужного инструмента.
- Чистый срез запрещён без подходящей capability.
- Вырвать целое растение можно руками, но качество падает до 55%.
- Runtime item сохраняет part, harvest damage, tool ID и quality.
- Сумка показывает часть и качество конкретного экземпляра.

### Recipe consequences

- Рецепт спорозрения требует `part_cap`, а не просто любой fungus.
- `CookingProcessEvent` переносит source quality.
- `RecipeResolver` учитывает технологическую точность и качество сырья.
- Аккуратно срезанная шляпка даёт `PURE`; повреждённый целый гриб — `UNSTABLE`.

### Observable station

- После `grind` в ступке появляется измельчённое сырьё.
- После `heat` меняются жидкость и анимированный пар котла.
- Визуал подписан на domain-события и не определяет результат рецепта.

### First forest chunk

- За физической дверью доступна отдельная low-poly поляна.
- Chunk содержит землю, коллизии деревьев, камни, свет и второй harvestable mooncap.
- Обратный portal возвращает игрока в убежище.
- Геометрический blockout заменён первым визуальным benchmark-проходом; это художественная база, а не финальный environment art.

### Verification

- `TRip smoke test: PASS`
- `TRip gameplay test: PASS` — включая переход на поляну.
- `TRip harvest quality test: PASS`
- `TRip cooking/effect test: PASS` — включая visual states станции.
- Поляна визуально проверена в Forward+/Metal.

## Expedition/knowledge vertical thread — implemented

### Field decisions

- `ToolbeltComponent` поддерживает набор инструментов и переключает их по stable content ID.
- `tool.spore_vial` даёт capability `collect_spores`; `Q` переключает нож и пробирку вместе с viewmodel.
- На поляне появилась `ingredient.false_mooncap` — опасный двойник с отдельным data definition, визуальным силуэтом и токсичными trait channels.
- Выбор `spores` реально требует пробирку, тогда как чистый срез шляпки требует нож.

### Knowledge and objective

- Осмотр через общий `InteractableComponent` создаёт semantic inspection event, не UI-хак.
- `KnowledgeOrchestrator` хранит уровни `UNKNOWN → OBSERVED → COLLECTED → UNDERSTOOD`.
- Полевой гербарий открывается на `J` и подписан на knowledge events.
- Первая цель требует правильный вид и часть `cap`, собранные именно в лесной зоне.
- Ложная лунница даёт объяснимую ошибку, но не двигает цель.
- После правильного образца цель меняется на возвращение; обратный portal завершает вылазку.

### Risk pressure

- `ExpeditionClock` выдаёт нормализованное время и фазы `DAY / DUSK / NIGHT`.
- Forest chunk сам интерпретирует фазу через свет и интенсивность свечения спор.
- Первый `ListenerCreature` принимает только `GameplayNoiseEvent`, различает investigate/alert и идёт к источнику.
- Шаги игрока подключены к слуху существа на уровне композиции ShelterLevel.

### Verification

- `TRip expedition systems test: PASS` — проверены гербарий, пробирка, ложный/верный образец, возвращение, фазы времени и реакция AI на шум.
- Все четыре прежних headless-теста продолжают проходить.
- Поляна повторно проверена GPU capture: в кадре видны оба вида гриба, listener и новый objective/clock HUD.

## Stealth vertical thread — implemented

- `PerceptionSensorComponent` объединяет настраиваемый cone/range, physics line-of-sight, слух и suspicion.
- Стены, деревья и камни действительно перекрывают зрение через collision query.
- Стояние, движение, бег и crouch дают различную сигнатуру заметности.
- `ListenerCreature` перешёл на состояния `IDLE / INVESTIGATE / ALERT / CHASE / SEARCH`.
- `StealthOrchestrator` связывает player и sensors на уровне зоны и агрегирует угрозу для HUD.
- HUD показывает состояния `СКРЫТ / ПОДОЗРЕНИЕ / ТРЕВОГА / ОБНАРУЖЕН` и шкалу накопления.
- На `G` бросается физический камень; удар создаёт `GameplayNoiseEvent`, расходует ограниченный запас и переносит внимание существа.
- `TRip stealth test: PASS` проверяет прямую видимость, преследование, снижение exposure в crouch и отвлечение ударом камня.

## Physical cooking vertical thread — implemented

- `ThermalVesselState` моделирует воду, текущую и пиковую температуру, уровень огня, выдержку в целевом окне, передержку, число перемешиваний и однородность.
- Слабый огонь стабилизируется в рабочем диапазоне; сильный быстрее нагревает, но может необратимо испортить смесь.
- Готовка разбита на физические affordances: кувшин, ступка, котёл, заслонка очага, мешалка и чистая склянка.
- Игрок должен налить воду, перенести измельчённый образец, выбрать огонь, перемешать и самостоятельно решить момент розлива.
- Resolver оценивает реальные измерения процесса: stir count, homogeneity и overheat duration.
- HUD сообщает наблюдаемые признаки вместо скрытого progress bar: температура, однородность, нужный режим, серебристый пар и запах гари.
- Жидкость меняет цвет от температуры, слабый/сильный огонь меняет свет, пар усиливает читаемость готовности.
- `CookingStationAudio` процедурно создаёт пространственный треск огня и частоту пузырьков без зависимости gameplay от аудио.
- `TRip physical cooking test: PASS` проверяет чистый состав на слабом огне и провал от сильной передержки.

## Interactive inspection vertical thread — implemented

- `InspectionClueDefinition` задаёт stable observation ID, ракурс, допуск и нужный zoom.
- `InspectionSession` независимо от UI вычисляет найденные признаки по вращению и приближению.
- Полноэкранный `SampleInspectionView` использует отдельный SubViewport и собственный World3D.
- Модель можно непрерывно вращать мышью, пошагово на `A/D` и приближать колесом.
- Для настоящего и ложного гриба созданы разные inspect-модели и по три морфологических признака.
- Гербарий хранит конкретные clue IDs и показывает прогресс признаков каждого вида.
- Полный набор переводит вид в `UNDERSTOOD`, а не просто факт открытия карточки.
- `HypothesisOrchestrator` проверяет data-driven required observations; гипотеза настоящей лунной шляпки подтверждается тремя совместными признаками.
- Мир не ставится на паузу во время полевого осмотра: скрытность и время продолжают создавать риск.
- `TRip inspection test: PASS` проверяет модель, ракурсы, zoom, знания и подтверждение гипотезы.

## Full loop and persistence vertical thread — implemented

- Создан checkpoint-коммит `2479e5a` после успешного прогона восьми тестов.
- `GameLoopOrchestrator` связывает вылазку, возвращение, физическую готовку, отчёт, награду и следующую цель.
- После возвращения objective меняется на приготовление чистого настоя; случайная готовка до вылазки не завершает цикл.
- Экран результатов показывает качество, точность, время, число изученных признаков и конкретную награду.
- Награда физически открывает ранее заблокированный portal в отдельный chunk глубокой рощи.
- `SessionPersistenceOrchestrator` сохраняет inventory instances, knowledge/clues, objective, clock, cooking process/vessel, toolbelt, loop/unlocks, player transform и собранные world spawns.
- Autosave с debounce запускается после сбора, изменения знаний и ключевых переходов цикла; ручная запись выполняется при возврате в меню.
- Главное меню активирует Continue при наличии slot `0`; новая игра создаёт чистое сохранение, Continue восстанавливает session.
- Save schema поднята до версии 2, payload остаётся data-driven и не содержит NodePath.
- `TRip full cycle/persistence test: PASS` проходит полный цикл, создаёт новый уровень, загружает save и проверяет route unlock, инвентарь, знания, позицию и отсутствие повторного spawn.

## Visual benchmark foundation — implemented

- Лес получил отдельный `BiomeVisualProfile`: зелёный ambient, туман, фон и плавный переход из тёплого профиля убежища.
- Поляна собрана из переиспользуемых `GnarledTree`, рельефных моховых масс, ломаной тропы, упавшего дерева, камней и пространственных ориентиров.
- `VegetationScatter` детерминированно создаёт траву и папоротник через два `MultiMeshInstance3D`, без сотен отдельных Nodes.
- Напольная поверхность использует собственную low-resolution forest-floor texture с nearest filtering; геометрия, свет и VFX остаются отдельными слоями.
- Добавлены GPU-споры, локальное грибное свечение и более читаемый многосоставной силуэт Listener.
- Убежище получило балки, рейки, доски пола, полки, бутылки, сушёные травы и физический фонарь.
- Лунная шляпка получила жабры, кольцо и пятна; ложный вид — отличимые наросты.
- Временный viewmodel заменён на низкополигональные рукава, округлые кисти и пальцы. Полноценный rig и анимации остаются следующим art-pass.
- Обе сцены проверены GPU capture в Forward+/Metal, все девять headless-тестов проходят.

## Northern trail and mycologist search — implemented

- Между поляной и глубокой рощей добавлен отдельный протяжённый `ForestTrailChunk`; маршрут больше не сводится к одному квадрату и телепорту в соседнюю коробку.
- Северная тропа содержит собственный terrain layer, MultiMesh-растительность, туманные плоскости, GPU-споры, крупные ориентиры и два позиционных procedural audio emitter.
- Порталы получили геометрическую раму, светящиеся швы и локальный свет: выход из убежища и переходы читаются как пространственные объекты.
- После первого цикла открывается северная тропа, но путь в рощу проявляется только во время активного `effect.spore_sight`.
- Настой теперь запускает анимацию питья, усиливает screen-space волны/VHS/chroma, открывает world-space ступени и повышает заметность игрока для Listener.
- Два интерактивных следа начинают поиск пропавшего миколога. Второй виден только под спорозрением; найденные clue IDs сохраняются в `GameLoopOrchestrator`.
- Objective HUD последовательно ведёт игрока: северная тропа → первая зарубка → принять настой → найти споровую запись → войти в дышащий проход.
- Кувшин, пестик и мешалка получили короткие physical-action анимации; состояние и результат готовки по-прежнему определяет domain-модель.
- `TRip full cycle/persistence test` дополнительно проверяет следы миколога, проявление маршрута и восстановление narrative progress.

## Deep grove second expedition — implemented

- Прежняя тестовая площадка 12×12 заменена зоной 30×30 с нижней чашей, наклонным подъёмом, верхней террасой и отдельными ветками исследования.
- Композицию держат гигантские грибы, древнее кольцо, светящийся подъём и лагерь на возвышении; земля использует общий forest material, а растительность — отдельный MultiMesh seed.
- В лагере есть палатка, лежанка, ящики, локальный свет и интерактивная свежая запись пропавшего миколога.
- Добавлен `ingredient.emberberry` и harvestable-сцена тлеющей ягоды с частями `berry / leaf / root / whole`, собственным свечением и двумя persistent spawn IDs.
- Вторая objective-цепь ведёт от входа в рощу к лагерю, затем к ягоде и возвращению в убежище.
- `GameLoopOrchestrator` сохраняет вход в рощу, лагерь, сбор ягоды и завершение второй вылазки.
- Убежище получило жилой угол и кладовую сразу; после первого цикла проявляется карта маршрутов, после второго — полка светящихся образцов.
- `TRip full cycle/persistence test` проходит обе экспедиции и проверяет восстановление лагеря, ягоды, инвентаря и финального objective state.
- Глубокая роща и развитое убежище проверены отдельными Forward+/Metal captures.

## Counteragent and spore tide — implemented

- `CookingOrchestrator` поддерживает массив `RecipeDefinition` и выбирает активный рецепт по `primary_ingredient_id`; центральная станция больше не привязана к одному грибу.
- Одна ступка автоматически выбирает доступный подходящий образец, а `ThermalVesselState` переносит ingredient tags через физический процесс и сохранение.
- Добавлены `recipe.emberberry_tonic`, `item.emberberry_tonic` и `effect.spore_quiet`.
- Эффекты получили data-driven `cancels_effect_ids`: спорозрение и контрагент взаимно исключаются без проверки конкретных ID в player/UI.
- Контрагент снижает stealth exposure, почти блокирует споровое заражение, закрывает mycelial route и включает приглушённый audio snapshot.
- `SporeTideOrchestrator` моделирует `CALM / RISING / SURGE`, нахождение игрока в зоне, накопление exposure, защиту препаратом и выброс ко входу при переполнении.
- Глубокая роща интерпретирует состояние прилива количеством GPU-спор и интенсивностью света; HUD показывает фазу и exposure.
- Спорозрение ускоряет заражение, создавая реальную цену за доступ к скрытой информации.
- `TRip counteragent/tide test: PASS` проверяет второй рецепт, отмену эффектов, исчезновение маршрута и разницу защищённого/незащищённого exposure.
- Полный persistence test теперь также готовит контрагент и восстанавливает новый preparation choice.

## Spore shelters and surge clue — implemented

- Добавлен переиспользуемый `SporeShelterZone` с радиусом и силой защиты; hazard вычисляет вклад ближайшего укрытия без знания его визуальной сцены.
- В глубокой роще размещены три крупных грибных навеса, создающих безопасную цепочку от нижней зоны к лагерю.
- Внутри укрытия exposure убывает даже при `SURGE`; HUD показывает конкретное название активного навеса.
- Прямая ветка к древнему кольцу остаётся открытой и быстрее, но не защищает от прилива.
- `NarrativeClue` получил общие requirements `requires_spore_vision + required_world_state`; недоступная находка выключает и visual, и collision.
- Координатный след миколога у кольца проявляется только при одновременных `effect.spore_sight` и `SporeTide.State.SURGE`.
- Найденный след сохраняется обычным stable clue ID и переводит цель на подготовку к корневому колодцу.
- `TRip counteragent/tide test` дополнительно проверяет снижение exposure в физическом укрытии и двухфакторное проявление следа.

## Next vertical thread

1. Заменить составную low-poly кисть на rigged first-person arms и анимации среза, измельчения, розлива и осмотра.
2. Добавить отдельные семейства грибных деревьев/кристаллов для глубокой рощи и корневого колодца.
3. Продолжить корневой колодец ниже ложного сердца и добавить встречу с микологом.
4. Расширить физический гербарий отдельными карточками изученных видов и образцов.
5. LOD/occlusion pass после профилирования нового плотного dressing.

## Physical investigation board — implemented

- В убежище добавлена коллизионная world-space доска расследования, доступная через общий `InteractableComponent`.
- Зарубка, лагерная запись и координаты кольца материализуются отдельными карточками; красные нити появляются только после финальной находки.
- После получения контрагента и координат игрок выбирает один из двух планов: защищённый длинный спуск или короткий резонансный маршрут с повышенным вниманием грибницы.
- Выбор хранится как semantic ID `warded_descent / resonant_descent`, меняет objective и не зависит от конкретного визуального представления доски.
- Выбранная карточка отмечается world-space маркером, а повторное взаимодействие переключает план без отдельного меню.
- План входит в обычное сохранение `GameLoopOrchestrator`; full-cycle test проверяет запрет раннего выбора, смену objective и восстановление физического маркера.

## Root well third expedition — first phase implemented

- Под древним кольцом появился отдельный вертикальный chunk корневого колодца с входной площадкой, несколькими уровнями глубины, корневыми арками, частицами и пространственным пульсом.
- Вход закрыт до выбора на доске; начало спуска фиксирует решение, чтобы геометрию нельзя было переключить под игроком.
- `warded_descent` включает длинный трёхсекционный маршрут и две физические защитные мембраны; `resonant_descent` включает короткий открытый спуск и световые импульсы-навигацию.
- Новый `RootPressureOrchestrator` моделирует `REST / LISTENING / HUNTING`, учитывает movement exposure, выбранный план и spatial protection zones.
- При переполнении давление не убивает игрока, а возвращает на входную площадку, сохраняя знание и выбранный план.
- HUD показывает состояние корней, давление и название активной мембраны только внутри зоны колодца.
- Внизу находится интерактивный сигнал: миколог жив, но ложное сердце уже имитирует его голос; stable clue ID сохраняется и меняет objective.
- `TRip root well test: PASS` проверяет переключение visual/collision веток, защиту мембраны, рост давления на открытом пути и блокировку смены плана после входа.

## Seeded biome visual foundation — implemented

- Добавлен `BiomeDressingScatter`: один модуль строит отдельные MultiMesh-пулы хвойных, широколиственных, сухих стволов, камней и валежника.
- Поляна, северная тропа и глубокая роща получили разные density/palette profiles; authored landmarks и gameplay corridors остаются фиксированными.
- Декоративный layout зависит от сохранённого `world_seed`: новый забег получает другую композицию, загрузка возвращает прежнюю.
- Ландшафтный dressing расширен за физические границы маленьких gameplay-площадок, создавая несколько планов глубины и непрерывный лес вокруг маршрута.
- Environment получил procedural sky, SSAO, SSIL, glow и volumetric fog; лес использует тёплый направленный свет против холодного ambient/fog fill.
- Трава и папоротники используют собственные seeded MultiMesh-пулы; палитра и размеры исправлены так, чтобы подлесок поддерживал масштаб, а не выглядел россыпью одинаковых кубов.
- Старые симметричные капсульные руки убраны. Нож и флакон получили компактные локальные grip-модели; полноценный skeletal rig остаётся отдельной задачей.
- `TRip biome generation test: PASS` проверяет повторяемость одинакового seed и изменение композиционной сигнатуры при новом seed.

## Continuous authored landscape — implemented

- Отдельные плоские основания поляны, тропы, террасы и рампы отключены; экспедиционная зона теперь лежит на одной коллизионной heightfield-сетке.
- Рельеф сочетает seed-шум с художественными правилами: стабильная стартовая поляна, читаемый коридор маршрута, чаша глубокой рощи, возвышение лагеря, плавный подход и окружающий горный борт.
- Маршрут, холодная почва рощи и тёплая площадка лагеря окрашиваются прямо в вершинах общей сетки, без наложенных плит.
- Декоративные MultiMesh-объекты проецируются на высоту общего рельефа; генератор больше не создаёт собственные плоскости, холмы и скальные призмы, способные пересечь убежище.
- Плоские прямоугольные папоротники заменены объёмными низкополигональными кустами; поваленные стволы уменьшены до масштаба окружения.
- Forest profile использует цветной ambient fill, атмосферную перспективу и более светлую палитру почвы, поэтому передний, средний и дальний планы не проваливаются в чёрный.
- `TRip biome generation test: PASS` дополнительно фиксирует authored-высоты поляны, тропы, лагеря и непрерывность подъёма.

## Streaming world and consciousness phases — implemented

- Единичный ландшафт 57×49 заменён потоковой сеткой чанков 30×30; центральный чанк строится синхронно, окружающие кольца дозированно подгружаются по мере движения игрока, дальние выгружаются.
- Высота бесшовно вычисляется в глобальных координатах из macro-noise, ridged detail и извилистой долины, поэтому границы чанков не определяют форму мира.
- Сюжетные пространства сохраняются как authored constraints поверх генерации: убежище, стартовая поляна, маршрут, глубокая роща, лагерь и корневой колодец не могут быть разрушены новым seed.
- Каждый чанк детерминированно выбирает семейство леса, распределяет MultiMesh-деревья и валуны, а часть чанков получает крупную точку интереса; одинаковый world seed воспроизводит мир.
- Активный `spore_vision` теперь переключает `world phase`: видимый terrain плавно пульсирует и меняет материал, небо и объёмный туман переходят в психоделический профиль, следующие чанки генерируют другое семейство крон, палитру камней и мицелиальные ориентиры.
- После окончания эффекта контроллер возвращает текущий базовый профиль — убежище или лес — вместо жёсткого сброса в один биом.
- Streaming API содержит безопасный `ensure_area_at()` для загрузки сохранений и быстрых переходов без провала игрока до появления коллизии.
- `TRip biome generation test: PASS` проверяет authored-высоты, появление центрального чанка, paging после перемещения и фазовый контракт.

## Eight-world progression catalog and developer mode — implemented

- Зафиксировано восемь слоёв одной местности: Явь, Мицелиальный хор, Багряный гон, Стеклянная изморозь, Пепельная тишина, Зеркальный разлив, Корневой сон и Сердце Дальнего леса.
- `WorldPhaseDefinition` хранит effect channel, стабилизирующий рецепт, visual profile, семейство геометрии, палитры объектов, тип POI и biases пещер/опасности.
- `WorldPhaseOrchestrator` загружает каталог, выбирает мир по реальным gameplay channels и предоставляет тот же путь переключения режиму разработчика.
- `F10` открывает кликабельную панель всех миров; `PageUp/PageDown` листают фазы, `R` меняет seed, `Backspace` снимает симуляцию.
- Для шести новых миров добавлены отдельные environment-профили; генератор меняет формы крон и палитры terrain/decor по выбранному определению.
- Полный run loop, исходы рецептов, мета-прогрессия лаборатории и сюжетный каркас поиска брата описаны в `docs/WORLD_AND_RUN_DESIGN.md`.
- `TRip world phase test: PASS` проверяет восемь уникальных упорядоченных определений, профили, developer override и выбор Мицелиального хора настоящим каналом настоя.

## Consciousness metamorphosis presentation — implemented

- Правильный расходник теперь запускает не мгновенную перекраску, а самостоятельное событие метаморфозы поверх смены `WorldPhase`.
- `WorldMetamorphosisDirector` подписан на общий фазовый оркестратор, поэтому одинаково работает для рецептов, сюжетных переходов и режима разработчика.
- Экранный переход использует цвет и семейство геометрии целевого мира: радиальная деформация, хроматический разрыв, движущееся кольцо и краткий цветовой ожог.
- Переход синхронизирован с плавным изменением terrain-материала, неба, тумана и регенерацией декораций загруженных чанков.
- На шине `Perception` процедурно синтезируется отдельный звуковой подъём; остальные шины на время проваливаются в snapshot `metamorphosis`.
- HUD называет слой сознания, после стабилизации возвращается правильный аудиорежим активного эффекта.
- `TRip world phase test: PASS` дополнительно проверяет запуск, целевой мир и чистое завершение перехода.

## First chained world formula — implemented

- Реализована первая последовательная цепь `Явь → Мицелиальный хор → Багряный гон`, а не независимое переключение фильтров.
- Новый кровавый роговик физически находится на северной тропе, но выключает visual и collision до активного спорозрения.
- Роговик имеет отдельный силуэт ветвистого гриба, части сбора, запах хищника, токсичность и короткое окно свежести.
- Его правильная формула — багряный костный отвар — требует чистой верхней развилки, узкого температурного окна и более тщательного перемешивания.
- Употребление отменяет прежний слой сознания и включает `crimson_drive`; `WorldPhaseOrchestrator` переводит стриминг и художественные правила в Багряный гон.
- Эффект даёт 22% скорости, но увеличивает заметность для существ на 45%: новый мир является решением с ценой, а не бесплатным ключом.
- Успешно приготовленная формула навсегда записывается в отдельный `RecipeKnowledgeOrchestrator`, сохраняется между забегами и выводится в журнале.
- `TRip crimson progression test: PASS` проверяет скрытый ингредиент, приготовление, запоминание рецепта, смену мира, преимущество, цену и persistence payload.

## Distinct biome content packs — implemented

- `WorldPhaseDefinition` теперь ссылается на отдельный `BiomeContentPack`, а не только на палитру и числовой geometry family.
- Все восемь миров получили уникальные ecology, vegetation и geology families, собственное ландшафтное высказывание, тип POI и по три локальные тайны.
- Смена сознания полностью перестраивает heightfield и collision: тайга превращается в карст, красную ступенчатую осыпь, ледниковый кар, пепельную тундру, озёрную топь, корневые провалы или финальное плато.
- Процедурные силуэты разнесены по семействам: хвойные, гигантские грибы, роговые лиственницы, ледяной лишайник, горельник, тростник, корневые кольца и согласованные рощи.
- Геология также не переиспользует один валун: гранит, карстовые рёбра, красная осыпь, ледяные кристаллы, пепельные столбы, береговые полки, корневые клубни и парящие пласты.
- Вода и пещерные устья генерируются только в подходящих мирах; POI несут semantic `poi_kind` и `mystery_id` для будущего нарратива.
- При перестройке поверхности игрок сохраняет безопасную высоту над землёй; крупные объекты соблюдают защитный радиус и не перекрывают первый план.
- Художественные правила, матрица восьми миров и бюджет авторских ассетов закреплены в `docs/BIOME_ART_BIBLE.md`.
- `TRip world phase test: PASS` требует восемь уникальных ecology/vegetation/geology signatures; Forward+/Metal capture сравнивает все восемь миров с одной точки маршрута.

## Authored biome mesh pass 01 — implemented

- Добавлен `BiomeMeshLibrary`: фасеточная геометрия строится один раз через `SurfaceTool` и инстансится потоковыми чанками через `MultiMesh`.
- Для Яви созданы изогнутые сбежистые стволы, нерегулярные ярусы хвои, отдельный гранит и травяной подлесок.
- Для Мицелиального хора созданы собственные ножки и кольцевые шляпки с нижней стороной, карстовые рёбра и светящийся грибной подлесок.
- `BiomeHorizon` следует за игроком независимо от чанков: природные фазы получают кольцо дальних горных масс, Хор — исполинские грибные силуэты.
- `BiomeAtmosphere` добавляет локальную движущуюся взвесь: сдержанную пыльцу в Яви и крупные эмиссивные споры в изменённом сознании.
- Presentation layers выключаются внутри дорожной лаборатории и восстанавливаются при выходе в экспедицию.
- Headless-тест проверяет обязательные groundcover, horizon и atmosphere contracts; обе первые фазы проверены Forward+/Metal capture.

## Authored biome mesh pass 02 — implemented

- Оставшиеся шесть миров переведены с универсальных `PrismMesh/TorusMesh` на собственные SurfaceTool-наборы растительности, геологии и groundcover.
- Багряный гон получил ветвящиеся рога, составную красную осыпь, степные колючки и горизонт костяных вилок.
- Стеклянная изморозь получила кристаллические кластеры разных высот, ледяные розетки, дальние ледяные пики и направленный снег.
- Пепельная тишина получила обломанные развилки, неровные пепельные столбы, обугленный вереск и мелкий пеплопад без экранных квадратов.
- Зеркальный разлив получил тростниковые веера, слоистые береговые полки, зависшие дальние острова и горизонтальную влажную взвесь.
- Корневой сон получил деформированные трубчатые петли, корневые клубни, ползущие усики и восходящие частицы.
- Сердце получило многоконтурные рощи, сложенные парящие пласты, кольцевой groundcover и отдельное движение атмосферы.
- `ExpeditionTerrain` кеширует сгенерированную topology по semantic key: стриминговые чанки переиспользуют Mesh и создают только собственные MultiMesh transforms.
- `TRip world phase test` требует groundcover, horizon и atmosphere для всех восьми фаз и проверяет заполнение mesh cache; восемь состояний повторно приняты Forward+/Metal capture.

## Terrain material and viewmodel polish — implemented

- Terrain shader больше не заменяет vertex palette почти сплошным фазовым цветом: базовая почва смешивается с палитрой сознания и сохраняет локальный характер биома.
- Добавлены детерминированная крупноячеистая микровариация, широкие цветовые полосы и отдельная склоновая маска; перепады heightfield читаются даже в монохромных мирах.
- Эмиссия altered terrain ослаблена и исключается на крутых склонах, поэтому геометрический объём не превращается в плоское свечение.
- Нож и пробирка уменьшены и опущены к краю кадра; grip palm и рукав приведены к человеческому масштабу и больше не перекрывают центр обзора.
- Обновлённые Явь, Багряный гон и Стеклянная изморозь проверены Forward+/Metal capture.

## Procedural biome ambience — implemented

- `BiomeProceduralAmbience` синтезирует независимый ambient bed на шине `Ambience`; gameplay по-прежнему не читает звук как состояние.
- Восемь ecology families используют разные фундаментальные частоты, гармоники, шумовую долю, пульсацию и стереофазу.
- Акустический профиль переключается тем же `WorldPhaseDefinition`, что terrain, horizon и weather, включая developer mode.
- Outdoor bed плавно фактически заглушается внутри дорожной лаборатории и возвращается после выхода в экспедицию.

## New-game expedition start and road laboratory

- Устранён жёсткий старт новой игры внутри старой комнаты: новая сессия помещает игрока непосредственно в Явь на процедурный terrain.
- Архитектура комнаты, дверь и старые portal-переходы отключаются для реальной сессии; старые saves мигрируют в проявленную переносную лабораторию возле сохранённой позиции.
- Котёл, ступка, вода, склянки, доска расследования и постоянные улучшения собраны под отдельным `RoadLaboratoryOrchestrator`.
- Первый физический cairn открывает лабораторию обрядом; проявление использует пространственную ring-метаморфозу. После открытия `L` убирает лагерь или проявляет его возле текущей позиции.
- Позиция, unlock и manifested state сохраняются отдельно. Возврат с образцом теперь завершается входом в радиус лагеря, а не portal-объектом.
- Добавлен отдельный headless contract test и Forward+/Metal capture `/tmp/trip_road_laboratory_capture.png`; полный набор из шестнадцати headless-тестов проходит.
- Headless world-phase test проверяет совпадение активного audio ecology family со всеми восемью мирами.

## Interactive world content chain

- Все восемь content packs получили авторскую интерактивную тайну, локальный ингредиент, переходную формулу и явную связь со следующим миром; финальный слой завершает цепь лабораторией брата.
- Реализованы пять отсутствовавших ингредиентов, рецептов, consumable и effects: стеклянный лишайник, пепельный колокольчик, зеркальный камыш, второй корень и шляпка согласия.
- `WorldProgressionOrchestrator` открывает рецепт как гипотезу после исследования POI, отличает гипотезу от освоенной формулы, обновляет HUD-задачу и сохраняет тайны, собранные procedural spawn IDs и самый глубокий story world.
- Употребление правильного состава перестраивает biome generation; после окончания острого эффекта достигнутый сюжетный слой не исчезает.
- POI резервируют свободную поляну и сглаженный участок heightfield до scatter pass. Багряный shrine, ледяной архив и исчезающая стоянка получили отдельные составные силуэты.
- Developer panel показывает landscape/ingredient/recipe/next-world contract; `Enter` и отдельная кнопка имитируют употребление правильной переходной формулы.
- `TRip world content progression test` проходит всю цепь из восьми миров, семь тайн, семь варок, семь употреблений и persistence contract.
- Тайны работают как пространственные микрособытия: пять разных правил поведения, давление и срыв контакта, синтезированный 3D-мотив, визуальные эхо и проявление локального ингредиента только после решения.
- Багряный POI получил движущихся ложных зверей, матовые несущие формы и холодный событийный контраст; narrative notice перенесён из строки основной цели в отдельную экранную зону.
- Developer panel умеет клавишей `P` или отдельной кнопкой разрешить ближайшее событие для быстрого QA. Полный набор из восемнадцати headless-тестов проходит.

## Art-directed infinite route

- Случайный landmark roll каждого чанка заменён продольным ритмом мира: POI возникают через заданное для биома число рядов и располагаются на кромке основной долины.
- Долина является частью heightfield, продолжается вместе со streaming и меняет изгиб по run seed и фазе сознания. Это не невидимый spline и не нарисованная дорога.
- Scatter сохраняет узкое ходовое ядро свободным, но оставляет растительность по бокам; terrain palette немного выделяет утоптанное направление без экранного маркера.
- `biome_generation_test` проверяет cadence, боковое расположение POI и физический перепад между дном маршрута и плечом ландшафта.

## Biome phenomena and staged metamorphosis

- Все восемь миров получили отдельный `BiomeHazardDefinition`: название, предупреждение, активную фазу, инструкцию, цену ошибки, длительности, давление, VFX family и procedural sound pitch.
- Реализованы пять способов контригры: замереть, продолжать идти, слушать пригнувшись, двигаться медленно и искать высокий рельеф.
- Ошибка возвращает к последней устойчивой позиции с частичным давлением, а не стирает знания или сюжетный прогресс. Состояние сохраняется и корректно мигрирует из старых save.
- Проявленная дорожная лаборатория стала настоящим полевым укрытием от любого явления.
- Screen shader получил независимый hazard channel; GPU-частицы и 3D-звук меняются по visual family. Developer mode запускает явление клавишей `H`.
- Метаморфоза разделена на onset, rupture и settle, длится дольше, сохраняет силуэты нового мира и получает разную звуковую гармонику для каждого geometry family.
- `TRip biome hazard test` проверяет восемь профилей, пять правил, рост и восстановление давления, overwhelm и persistence. Полный набор из девятнадцати headless-тестов проходит.

## Ecology compositions and volumetric lighting

- Восемь биомов получили восемь неповторяющихся `composition_family` с собственным пространственным мотивом.
- Крупные микролокации ставятся художественным ритмом между сюжетными точками, а не независимым случайным посевом.
- Растительность, геология и напочвенный покров собираются разными масками в пятна и оставляют читаемые просветы.
- Профиль мира управляет направлением и цветом ключевого света; чрезмерный ambient и emission снижены ради теней, глубины и читаемого силуэта.
- Автоматический тест проверяет уникальность композиций, их фактическую сборку и отдельный свет каждого мира.
- Массовая растительность получила GPU motion profile: хвоя качается, грибы медленно дышат, тростник гнётся, корни пульсируют, а лёд сохраняет почти неподвижный силуэт.

## Interface, movement and QA pass

- Инвентарь, журнал и осмотр получили взаимоисключающее владение мышью и блокируют world interaction; Escape сначала закрывает верхний интерфейс, затем ставит игру на паузу.
- Журнал прокручивается, HUD природного явления, локального давления и нарративного сообщения разведены по отдельным экранным полосам.
- Остановка больше не перематывает фазу head bob: амплитуда плавно затухает. Viewmodel учитывает фактическую скорость, приседание и sprint lowering; crouch footsteps стали тише и короче.
- Gamepad look не проходит сквозь открытый интерфейс. FOV применяется к активной камере сразу; главное меню получило настройки FOV, head bob, сброс и закрытие через Escape.
- Лаборатория включает collision и interaction только после завершения локальной метаморфозы, а при исчезновении отключает их до сжатия. Эффект получил объёмные споры и отдельную стадию пространственного кольца.
- Developer panel стал прокручиваемым QA-пультом: прямой выбор 1–8, формула, POI, hazard start/reset, лаборатория animated/instant, маршрутные телепорты, локальный образец, время, seed и ручное сохранение.
- `interface_developer_qa_test` проверяет ходьбу, приседание, bob, UI ownership, FOV, экранные конфликты, developer actions, лабораторию, hazard reset и паузу.
- Полный набор из двадцати headless-тестов проходит; settings, developer panel и обе стадии лаборатории приняты отдельными Forward+/Metal кадрами.

## Biome fauna, NPC sheets and lore boundary

- Добавлены 18 data-driven паспортов живности: реальные виды Алтая, микрофауна, птицы-навигационные ориентиры и явно маркированные авторские сущности слоёв сознания.
- Все восемь миров имеют минимум два допустимых вида; фауна выбирается детерминированно из seed и координаты загруженного чанка и полностью переселяется при смене слоя.
- Процедурные low-poly actors исполняют forage/wander/notice/flee/stalk/observe, двигают корпус, голову, конечности или крылья и следуют authored темпераменту и дистанциям вида.
- Охраняемые аргали и снежный барс не имеют охотничьей награды: их функция — наблюдение, навигация и полевой атлас.
- Созданы 8 полных NPC-паспортов с одеждой, снаряжением, выражениями, анимациями, целью, потребностью, секретом и encounter rules.
- `FAUNA_NPC_AND_LORE_BIBLE.md` разделяет документальную природу, современных вымышленных людей и собственную мифоэкологию TRip. Реальные обряды и сакральные символы не становятся рецептами или пазлами без культурной консультации.
- Developer panel показывает активную фауну; `K` переселяет виды, `N` переключает плотность.
- `TRip biome population test` проверяет каталоги, character-sheet validation, покрытие восьми миров, фактическое заселение чанков, смену состава при метаморфозе и developer density contract.
- Полный набор из двадцати одного headless-теста проходит; runtime-фауна дополнительно принята Forward+/Metal capture `/tmp/trip_biome_population_capture.png`.

## First-person controller and rigged player body

- Подключён CC0 `Animated Human` от Quaternius: скелет, low-poly mesh и девять импортированных клипов. Оригинальная лицензия, ссылка и палитры хранятся вместе с asset.
- `PlayerAvatarAnimator` переключает Idle/Walk/Run/Jump/Working по реальному состоянию контроллера с коротким blend и speed scaling. Тело отбрасывает анимированную тень, но не клипует first-person camera.
- Аналоговый input больше не нормализуется до полной скорости; добавлены отдельное торможение, forward-only sprint и сохранение фактической скорости походки.
- Добавлены Space/B jump, input buffer, coyote time, crouch jump lock, floor snap, slope limit и автоматическое преодоление низких препятствий после проверки свободного объёма и поверхности сверху.
- Камера получила небольшой поворот от боковой скорости и затухающий landing spring. Viewmodel реагирует на дыхание, взгляд, фактическую походку, спринт, присед и приземление.
- CharacterBody ограниченно толкает лёгкие `RigidBody3D`, используя скорость до разрешения столкновения; сила ограничена, чтобы мелкий реквизит не выстреливал.
- `PlayerFoley` синтезирует вариативные шаги, отрыв и приземление на шине `PlayerFoley`; громкость шага следует crouch/walk/sprint intensity, а посадка — фактической вертикальной скорости.
- `player_avatar_import_test` фиксирует Skeleton3D, AnimationPlayer и набор клипов. `player_controller_test` проверяет аналоговую скорость, торможение, спринт, прыжок, приземление, crouch capsule, стену, низкую ступень, rigid-body push и animation states.
- Rig и walk animation приняты Forward+/Metal capture `/tmp/trip_player_avatar_capture.png`.

## Terrain audit remediation and authored taiga kit

- Первая часть `ExpeditionTerrain` вынесена в отдельный `TerrainChunkMeshBuilder`. Высота теперь вычисляется по общей решётке с однократной рамкой соседних samples вместо пяти вызовов на каждую вершину; форма, индексы, vertex colors и физический контракт terrain не изменились.
- Старые чанки, decor и presentation layers сразу отсоединяются от SceneTree перед deferred free. При смене мира старые collision/render nodes больше не живут один кадр одновременно со своими заменами.
- Дальние Mirror, Root и Heart silhouettes получили безопасные диапазоны радиуса и масштаба. Сердце использует собственные loop-массы вместо гигантских floating strata, перекрывавших камеру.
- Подключён отобранный CC0 Kenney Nature Kit 2.1: 48 вариантов сосен, валунов, бурелома, пней, кустов, трав и грибов. Процедурные MultiMesh остаются дешёвым заполнением, а импортированные сцены дают редкие authored silhouettes.
- `AuthoredNatureAssetLibrary` кеширует PackedScene, удаляет экспортные камеры/свет и нормализует масштаб по семействам. Автотест фиксирует наличие мешей и безопасные итоговые размеры: около 7 м для высокой сосны, 5.5 м для округлой, 1.5 м для валуна и 0.4 м для грибной группы.
- `BiomeVisualProfile` теперь управляет не только верхом неба, но и нижней полусферой ProceduralSky. Базовая Явь получила отдельный естественный профиль неба, утреннего ключевого света, воздушной перспективы и почвы; изменённые состояния сохраняют свои кислотные profiles.
- Нарративное сообщение поднято из зоны прицела в верхнюю полосу, а сравнительный visual capture скрывает его, чтобы проверять именно композицию мира.

## Field inventory and expedition archive

- Полевая сумка показывает компактные категории и сохраняет отдельные физические экземпляры внутри визуального stack: часть растения, качество и свежесть больше не теряются за общей строкой.
- Каталог сортируется по названию, качеству, количеству и свежести; правая колонка сравнивает лучший образец, среднюю свежесть, массу и объём.
- Полевой гербарий перестроен в широкий архив с тремя независимыми разделами: виды, гипотезы и формулы.
- Карточка вида показывает уровень знания, число образцов в сумке, среду и каждый морфологический признак; неизвестные признаки не раскрывают текст заранее.
- Гипотезы отображают фактическую цепочку доказательств, связанные образцы и состояние проверки. Формулы различают найденную гипотезу и освоенный рецепт.
- UI читает существующие `InventoryComponent`, `KnowledgeOrchestrator`, `HypothesisOrchestrator` и `RecipeKnowledgeOrchestrator`; save schema не изменён и gameplay truth не перенесён в виджеты.
- Быстрые повторные уведомления больше не накапливают вертикальное смещение и не сползают в HUD опасности.
- `interface_developer_qa_test` проверяет specimen catalog, карточки видов, эксклюзивное владение интерфейсом и возврат управления. Добавлены отдельные Forward+/Metal captures сумки и гербария.

## Biome time, route identity and taiga value pass

- `BiomeVisualProfile` получил отдельные сумеречные и ночные состояния: горизонт, ambient, ключевой свет, туман, экспозиция и направление ночного света задаются каждым миром отдельно.
- `ExpeditionClock` теперь действительно двигает световую драматургию мира, а не только меняет текст HUD. Переход день → сумерки → ночь непрерывен и не мутирует общие Resource.
- Восемь `BiomeContentPack` получили собственную геометрию маршрута: ширину, извилистость, вертикальный ритм и период обзорных точек. Красная степь стала широкой и прямой, грибной карст и корневой сон — теснее и лабиринтнее, зеркальная долина — широкой и плавной.
- Высота ходового ядра теперь следует художественной продольной кривой и формирует долины/обзорные гребни; случайный macro noise больше не может постоянно закрывать дальний план маршрута.
- Исправлено смешивание instance color в foliage shader: тёмный biome tint больше не перемножается с тёмной кроной до почти чёрного значения.
- Явь перебалансирована в пользу CC0 Kenney Nature Kit: на ближних чанках больше авторских сосен, напочвенных групп и валежника, а повторяющийся процедурный MultiMesh отступает в средний/дальний план.
- Добавлен тройной Forward+/Metal capture Яви для дня, сумерек и ночи. `biome_generation_test` проверяет уникальность навигационного ритма восьми миров; interface QA фиксирует реальное изменение света и экспозиции ночью.
